{
  "name": "careerstory-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Edge sidecar for CareerStory hosting. TLS gate, Stripe webhook, healthz.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "stripe": "^17.5.0"
  }
}
